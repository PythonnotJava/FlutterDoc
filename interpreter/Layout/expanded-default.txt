super.key: 这是继承自父类的key属性，用于在Widget树中唯一标识Expanded。
super.flex: 这是继承自父类的flex属性，用于指定Expanded在可用空间中的分配比例。默认值为1，如果多个Expanded具有不同的flex值，它们将根据flex的比例分配剩余空间。例如，如果一个Expanded的flex为2，而另一个Expanded的flex为1，则前者将获得后者的两倍剩余空间。
required super.child: 这是继承自父类的child属性，用于指定Expanded的子组件。它是一个必需的参数。