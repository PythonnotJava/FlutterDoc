transform: 用于定义组件的变换矩阵。可以通过 Matrix4 来设置各种变换效果，比如旋转、缩放、平移和倾斜等。
origin: 设置变换的原点，相对于组件左上角的偏移量。若不设置，默认原点在左上角 (0, 0)。
alignment: 设置变换的对齐方式，控制变换相对于组件的哪个位置发生。
transformHitTests: 控制是否对变换后的组件区域进行点击测试。
filterQuality:  设置图像在缩放或旋转时的渲染质量，通常用于提高图像清晰度。
child: 要进行变换的子组件。